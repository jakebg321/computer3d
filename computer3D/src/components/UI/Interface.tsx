import { FC } from 'react'

const Interface: FC = () => {
  return (
    <div style={{
      position: 'absolute',
      top: 0,
      left: 0,
      width: '100%',
      height: '100%',
      pointerEvents: 'none',
    }}>
      {/* UI elements will go here */}
    </div>
  )
}

export default Interface
